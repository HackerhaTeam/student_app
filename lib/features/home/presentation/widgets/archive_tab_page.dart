// صفحة الرئيسية

import 'package:flutter/material.dart';

class ArchiveTabPage extends StatelessWidget {
  const ArchiveTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('صفحة المحفوظات'));
  }
}
